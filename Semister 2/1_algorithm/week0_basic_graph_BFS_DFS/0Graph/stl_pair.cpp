#include <bits/stdc++.h>
using namespace std;

int main(){
    pair<string, int> p;
    p = make_pair("bijoy", 10);

    cout << p.first << " " << p.second << endl;
    return 0;
}

// pair is a build in class which can store same or two types of data in a single varable;